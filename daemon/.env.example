# ===========================================
# Claude Code Slack — Daemon Configuration
# ===========================================
# Copy to .env and fill in your values.
# NEVER commit .env to git!

# GitHub — the repo Claude operates on (needs 'repo' scope for private repos)
GITHUB_TOKEN=ghp_your-token-here
GITHUB_REPO=youruser/your-repo
GITHUB_BRANCH=main

# Slack — bot token for posting results
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Local workspace path — the CLONED repo where Claude Code runs
# IMPORTANT: Point this at a separate clone, NOT your working directory
WORKSPACE_DIR=/path/to/your/cloned/repo

# Claude Code CLI — full path if not in system PATH
# Find yours with: which claude
CLAUDE_BIN=claude

# Task timeout in seconds (default 5 minutes)
CLAUDE_TIMEOUT=300

# How often to check for new tasks (seconds)
POLL_INTERVAL=5
